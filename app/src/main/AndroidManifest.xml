<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kg.programm.programmingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_newicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_newicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Programmingapp">
        <activity android:name=".ui.profile.EditProfileActivity"></activity>
        <activity android:name=".ui.home.test.ResultsActivity" />
        <activity android:name=".ui.home.test.TestActualActivity" />
        <activity android:name=".ui.home.test.TestsActivity" />
        <activity android:name=".ui.home.tasks.TaskDetailActivity" />
        <activity android:name=".ui.home.tasks.TasksActivity" />
        <activity android:name=".ui.home.info.HomeInfoActivity" />
        <activity
            android:name=".ui.lecture.VideoFullActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".ui.lecture.LecturesActivity" />
        <activity android:name=".ui.login.setup.SetupProfileActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.splash.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>